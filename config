#!/usr/bin/env bash
export PLUGIN_COMMAND_PREFIX="ready"
export DOKKU_READY_BIN=${DOKKU_READY_BIN:=dokku}
export DOKKU_READY_DEFAULT_PORT=${DOKKU_READY_DEFAULT_PORT:=5000}
export DOKKU_READY_BOOTSTRAP_BASE_IMAGE=${DOKKU_READY_BOOTSTRAP_BASE_IMAGE:=alpine:3.20}
export DOKKU_READY_BOOTSTRAP_IMAGE_PREFIX=${DOKKU_READY_BOOTSTRAP_IMAGE_PREFIX:=dokku-ready-bootstrap}
export DOKKU_READY_POSTGRES_PLUGIN_URL=${DOKKU_READY_POSTGRES_PLUGIN_URL:=https://github.com/dokku/dokku-postgres.git}
export DOKKU_READY_LETSENCRYPT_PLUGIN_URL=${DOKKU_READY_LETSENCRYPT_PLUGIN_URL:=https://github.com/dokku/dokku-letsencrypt.git}
